import random
import string

WORDLIST_FILENAME = "words.txt"

def loadWords():
    """
    Returns a list of valid words. Words are strings of lowercase letters.
    
    words.txt contains 55,900 words, this function may
    take a while to finish.
    """
      print "Loading word list from file..."
    # inFile: file
    inFile = open(WORDLIST_FILENAME, 'r', 0)
    # line: string
    line = inFile.readline()
    # wordlist: list of strings
    wordlist = string.split(line)
      print "  ", len(wordlist), "words loaded."
    return wordlist#function definition and formal parameter
def is_get_guessed_word(l):
  """
  Input: l, a guessed letter
  Returns 
  """
print ("letter_is_in_word)
print(is_word_guessed(secret_word, letters_guessed))
return()

    """ 
    #get guessed word
 def is_get_guessed_word(word):
  """
  Input:import string, word from doc
  returns: guess_a_letter
  if is_letter(l):
    print(l, "you got it")
  else:
    print get available_letters(letters_guessed)
   
  
    
  
